/**
 * HQ content model — BRIEF.md §6. This is the schema judges read: every type carries
 * descriptions, sensible validation and evidence fields.
 * TODO(NEC-05): seance, exhumedPage, schemaProposal (+ proposedType/proposedField objects),
 *               question, task, redirectLedgerEntry.
 */
import type {SchemaTypeDefinition} from 'sanity'

export const hqSchemaTypes: SchemaTypeDefinition[] = []
