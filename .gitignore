node_modules
.env
.env.*
!.env.example
dist
.next
.sanity
.netlify
# Regenerated by build:drain-fn; committed so Netlify always has a worker even if
# the build step is skipped. Force-add after rebuild: git add -f netlify/functions/drain-background.mjs
netlify/functions/drain-background.js
netlify/functions/package.json
*.tsbuildinfo
.DS_Store
coverage
functions/**/.build/
